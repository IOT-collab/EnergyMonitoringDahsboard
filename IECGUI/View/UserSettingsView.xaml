<UserControl x:Class="IECGUI.View.UserSettingsView"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="1080" d:DesignWidth="1920"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#081421">

    <UserControl.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#48D9FF"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F4FAFF"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#A9D8EC"/>

        <Style x:Key="SettingsCard" TargetType="Border">
            <Setter Property="Background" Value="#B312263A"/>
            <Setter Property="BorderBrush" Value="#667ECBE5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#2BE7FF" BlurRadius="20" ShadowDepth="0" Opacity="0.16"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Height" Value="58"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Background" Value="#D0173048"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="#557ECBE5"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="#334F7892"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="20"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#7048D9FF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Height" Value="62"/>
            <Setter Property="Background" Value="#4014263A"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#64203950"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5548D9FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#7048D9FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#15101F32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderBrush" Value="#7ECAE2"/>
            <Setter Property="Padding" Value="8,3"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#182B40"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#7ECAE2"/>
            <Setter Property="Padding" Value="8,3"/>
        </Style>
    </UserControl.Resources>

    <Viewbox Stretch="Uniform">
        <Grid Width="1920" Height="1080" ClipToBounds="True">
            <Image Source="pack://application:,,,/IECGUI.Visuals;component/Images/Backend.jpg"
                   Stretch="UniformToFill" Opacity="0.62"/>
            <Rectangle Fill="#B804111D"/>
            <Rectangle Opacity="0.45">
                <Rectangle.Fill>
                    <RadialGradientBrush Center="0.5,0.15" GradientOrigin="0.5,0.15" RadiusX="0.86" RadiusY="0.66">
                        <GradientStop Color="#37325E79" Offset="0"/>
                        <GradientStop Color="#00030A12" Offset="1"/>
                    </RadialGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Grid Margin="64,50,64,48">
                <Grid.RowDefinitions>
                    <RowDefinition Height="118"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="104"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="User Settings" FontSize="42" FontWeight="Bold"
                                   Foreground="{StaticResource TextBrush}"/>
                        <TextBlock Text="Manage application users, access roles, and account status."
                                   FontSize="19" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="1" Command="{Binding HomeCommand}"
                            Style="{StaticResource GlassButton.Cyan}"
                            Width="190" Height="60" FontSize="21" FontWeight="SemiBold"
                            Content="⌂  Home" VerticalAlignment="Center"/>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource SettingsCard}" Padding="22">
                    <DataGrid ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                              AutoGenerateColumns="False" CanUserAddRows="False"
                              AlternationCount="2" HeadersVisibility="Column"
                              GridLinesVisibility="All" BorderThickness="0"
                              Background="Transparent" Foreground="{StaticResource TextBrush}"
                              RowHeaderWidth="0" SelectionMode="Single" SelectionUnit="FullRow">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Username"
                                                Binding="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                                                Width="*"/>
                            <DataGridTextColumn Header="Password"
                                                Binding="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                                                Width="*"/>
                            <DataGridComboBoxColumn Header="Role"
                                                    SelectedItemBinding="{Binding Role, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    ItemsSource="{Binding DataContext.Roles, ElementName=Root}"
                                                    Width="340"/>
                            <DataGridCheckBoxColumn Header="Enabled"
                                                    Binding="{Binding IsEnabled, Mode=TwoWay}"
                                                    Width="180"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Grid Grid.Row="2" Margin="0,22,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="＋  Add User" Width="190" Height="62" FontSize="20"
                                Command="{Binding AddUserCommand}" Style="{StaticResource GlassButton.Ghost}" Margin="0,0,16,0"/>
                        <Button Content="Delete User" Width="190" Height="62" FontSize="20"
                                Command="{Binding DeleteUserCommand}" Style="{StaticResource GlassButton.Red}" Margin="0,0,16,0"/>
                        <Button Content="Save Changes" Width="210" Height="62" FontSize="20" FontWeight="SemiBold"
                                Command="{Binding SaveCommand}" Style="{StaticResource GlassButton.Blue}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Viewbox>
</UserControl>
