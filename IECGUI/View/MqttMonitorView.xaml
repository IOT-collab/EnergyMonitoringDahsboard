<UserControl x:Class="IECGUI.View.MqttMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:IECGUI.Converters"
             xmlns:extensions="clr-namespace:IECGUI.Common.WPFExtensions;assembly=IECGUI.WPFExtensions"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="1080" d:DesignWidth="1920"
             Background="#081421" Unloaded="OnUnloaded">
    <UserControl.Resources>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <Style x:Key="MqttLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#B9E9F7"/><Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/><Setter Property="Margin" Value="0,3,8,3"/>
        </Style>
        <Style x:Key="MqttTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="White"/><Setter Property="Background" Value="#3314263A"/>
            <Setter Property="BorderBrush" Value="#667ECBE5"/><Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,2,0,6"/><Setter Property="MinHeight" Value="30"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
        <Style x:Key="LoginPasswordBox" TargetType="PasswordBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#3314263A"/>
            <Setter Property="BorderBrush" Value="#667ECBE5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,2,0,6"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="58"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <DockPanel Grid.Row="0" LastChildFill="True">
            <Button Content="◄ Back" Command="{Binding BackCommand}" Style="{StaticResource GlassButton.Cyan}" Height="42" Width="130" DockPanel.Dock="Left"/>
            <TextBlock Text="{Binding Configuration.DisplayName}" Foreground="White" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="20,0,0,0"/>
            <Border DockPanel.Dock="Right" CornerRadius="14" Padding="14,6">
                <TextBlock Text="{Binding StatusMessage }" Foreground="{Binding IsConnected, Converter={StaticResource BoolToColorConverter}}" FontSize="20"  HorizontalAlignment="Left" Margin="150,0,0,0" VerticalAlignment="Center"/>
            </Border>
        </DockPanel>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>

                <!-- SCADA Tab Style -->
                <Style TargetType="TabItem">

                    <Setter Property="Background" Value="#6614263A"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="#667ECBE5"/>
                    <Setter Property="FontSize" Value="20"/>
                    <Setter Property="Padding" Value="20,8"/>

                    <Setter Property="Template">

                        <Setter.Value>

                            <ControlTemplate TargetType="TabItem">

                                <Border x:Name="Border"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="5,5,0,0"
                      Margin="2,2,2,0">

                                    <ContentPresenter
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      ContentSource="Header"
                      Margin="10,4"/>

                                </Border>

                                <ControlTemplate.Triggers>

                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border"
                              Property="Background"
                              Value="#235C77"/>
                                    </Trigger>

                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border"
                              Property="Background"
                              Value="#1A3448"/>
                                    </Trigger>

                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border"
                              Property="Background"
                              Value="#2B6B85"/>
                                    </Trigger>

                                </ControlTemplate.Triggers>

                            </ControlTemplate>

                        </Setter.Value>

                    </Setter>

                </Style>

            </TabControl.Resources>
            <TabItem Header="Monitor">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions><RowDefinition Height="230"/><RowDefinition Height="*"/><RowDefinition Height="150"/></Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#6614263A" BorderBrush="#667ECBE5" BorderThickness="1" CornerRadius="6" Padding="10">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="34"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <DockPanel><TextBlock Text="LIVE MAPPED PARAMETERS" Foreground="White" FontSize="17" FontWeight="Bold"/>
                                <TextBlock Text="Values extracted from configured JSON paths" Foreground="#8CCEDF" FontSize="15" Margin="50,0,0,0" DockPanel.Dock="Right"/></DockPanel>
                            <DataGrid Grid.Row="1" ItemsSource="{Binding LiveReadings}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" Foreground="White" RowBackground="#3314263A" AlternatingRowBackground="#44203349" GridLinesVisibility="Horizontal">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Topic" Binding="{Binding Topic}" Width="2*"/>
                                    <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="170"/>
                                    <DataGridTemplateColumn Header="Parameters" Width="5*"><DataGridTemplateColumn.CellTemplate><DataTemplate>
                                        <ItemsControl ItemsSource="{Binding Fields}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate>
                                            <Border BorderBrush="#557ECBE5" BorderThickness="1" CornerRadius="3" Padding="6,2" Margin="2"><TextBlock Foreground="#C9F5FF" FontSize="12"><Run Text="{Binding Name}"/><Run Text=": "/><Run Text="{Binding Value}"/><Run Text=" {Binding Unit}"/></TextBlock></Border>
                                        </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                                    </DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                    <Border Grid.Row="1" Margin="0,10" Background="#6614263A" BorderBrush="#667ECBE5" BorderThickness="1" CornerRadius="6" Padding="10">
                        <Grid><Grid.RowDefinitions><RowDefinition Height="42"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <DockPanel><TextBlock Text="MESSAGE HISTORY" Foreground="White" FontSize="17" FontWeight="Bold" VerticalAlignment="Center"/><Button Content="Clear" Command="{Binding ClearHistoryCommand}" Style="{StaticResource GlassButton.Ghost}" Width="100" Height="30" DockPanel.Dock="Right"/><ComboBox ItemsSource="{Binding PayloadFormats}" SelectedItem="{Binding SelectedPayloadFormat}" Style="{StaticResource GlassComboBox}" Width="130" Height="32" Margin="8,0" DockPanel.Dock="Right"/><TextBox Text="{Binding MessageFilter, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}" Width="220" Height="32" Margin="8,0" ToolTip="Filter by topic or payload" DockPanel.Dock="Right"/></DockPanel>
                            <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredMessageHistory}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" Foreground="White" RowBackground="#3314263A" AlternatingRowBackground="#44203349" GridLinesVisibility="Horizontal">
                                <DataGrid.Columns><DataGridTextColumn Header="Time" Binding="{Binding ReceivedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss.fff}}" Width="190"/><DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="100"/><DataGridTextColumn Header="Topic" Binding="{Binding Topic}" Width="2*"/><DataGridTemplateColumn Header="Payload" Width="5*"><DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding FormattedPayload}" TextWrapping="Wrap" FontFamily="Consolas" Foreground="#C8F8FF"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn></DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                    <Border Grid.Row="2" Background="#6614263A" BorderBrush="#667ECBE5" BorderThickness="1" CornerRadius="6" Padding="10">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="220"/><ColumnDefinition Width="*"/><ColumnDefinition Width="90"/><ColumnDefinition Width="110"/><ColumnDefinition Width="130"/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition Height="32"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                            <TextBlock Grid.Column="0" Text="Publish topic" Style="{StaticResource MqttLabel}"/><TextBlock Grid.Column="1" Text="Payload" Style="{StaticResource MqttLabel}"/><TextBlock Grid.Column="2" Text="QoS" Style="{StaticResource MqttLabel}"/><TextBlock Grid.Column="3" Text="Retain" Style="{StaticResource MqttLabel}"/>
                            <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding PublishTopic, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}"/><TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PublishPayload, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}" AcceptsReturn="True" TextWrapping="Wrap"/><ComboBox Grid.Row="1" Grid.Column="2" ItemsSource="{Binding QualityOfServiceLevels}" SelectedItem="{Binding PublishQualityOfService}" Style="{StaticResource GlassComboBox}" Height="32" Margin="0,2,8,6"/><CheckBox Grid.Row="1" Grid.Column="3" IsChecked="{Binding PublishRetain}" Foreground="White" VerticalAlignment="Center"/><Button Grid.Row="1" Grid.Column="4" Content="Publish" Command="{Binding PublishCommand}" Style="{StaticResource GlassButton.Cyan}" Height="36"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Configuration">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                            <Border Background="#6614263A" BorderBrush="#667ECBE5" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,12">
                                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="BROKER CONNECTION" Foreground="White" FontWeight="Bold" FontSize="17" Margin="0,0,0,10"/>
                                    <TextBlock Grid.Row="1" Text="Display name" Style="{StaticResource MqttLabel}"/><TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Configuration.DisplayName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}"/>
                                    <TextBlock Grid.Row="2" Text="Host / URL" Style="{StaticResource MqttLabel}"/><TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Configuration.Host, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}"/>
                                    <TextBlock Grid.Row="3" Text="Port" Style="{StaticResource MqttLabel}"/><TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Configuration.Port, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}"/>
                                    <TextBlock Grid.Row="4" Text="Client ID" Style="{StaticResource MqttLabel}"/><TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Configuration.ClientId, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}"/>
                                    <TextBlock Grid.Row="5" Text="Username" Style="{StaticResource MqttLabel}"/><TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Configuration.Username, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}"/>
                                    <TextBlock Grid.Row="6" Text="Password" Style="{StaticResource MqttLabel}"/><PasswordBox Grid.Row="6" Grid.Column="1" Margin="0,2,0,6" extensions:PasswordBoxHelper.BindPassword="True" extensions:PasswordBoxHelper.BindablePassword="{Binding Configuration.Password, Mode=TwoWay}" Style="{StaticResource LoginPasswordBox}"/>
                                    <TextBlock Grid.Row="7" Text="Keep alive (sec)" Style="{StaticResource MqttLabel}"/><TextBox Grid.Row="7" Grid.Column="1" Text="{Binding Configuration.KeepAliveSeconds, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}"/>
                                    <CheckBox Grid.Row="8" Grid.ColumnSpan="2" Content="Use TLS / SSL" IsChecked="{Binding Configuration.UseTls}" Foreground="White" Margin="0,4"/><CheckBox Grid.Row="9" Grid.ColumnSpan="2" Content="Allow untrusted certificate (development only)" IsChecked="{Binding Configuration.AllowUntrustedCertificates}" Foreground="#FFD36A" Margin="0,4"/>
                                </Grid>
                            </Border>
                            <Border Background="#6614263A" BorderBrush="#667ECBE5" BorderThickness="1" CornerRadius="6" Padding="14"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/><ColumnDefinition Width="105"/></Grid.ColumnDefinitions><TextBlock Grid.Column="0" Text="CA certificate (optional)" Style="{StaticResource MqttLabel}"/><TextBox Grid.Column="1" Text="{Binding Configuration.CertificatePath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MqttTextBox}"/><Button Grid.Column="2" Content="Browse" Command="{Binding BrowseCertificateCommand}" Style="{StaticResource GlassButton.Ghost}" Height="32" Margin="8,2,0,6"/></Grid></Border>
                            <StackPanel Orientation="Horizontal" Margin="0,12,0,0"><Button Content="Save configuration" Command="{Binding SaveConfigurationCommand}" Style="{StaticResource GlassButton.Cyan}" Height="38" Width="180" Margin="0,0,8,0"/><Button Content="Connect" Command="{Binding ConnectCommand}" Style="{StaticResource GlassButton.Green}" Height="38" Width="130" Margin="0,0,8,0"/><Button Content="Disconnect" Command="{Binding DisconnectCommand}" Style="{StaticResource GlassButton.Red}" Height="38" Width="130"/></StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <Border Background="#6614263A" BorderBrush="#667ECBE5" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,12"><Grid><Grid.RowDefinitions><RowDefinition Height="34"/><RowDefinition Height="220"/><RowDefinition Height="38"/></Grid.RowDefinitions><DockPanel><TextBlock Text="SUBSCRIPTIONS" Foreground="White" FontSize="17" FontWeight="Bold"/><StackPanel Orientation="Horizontal" DockPanel.Dock="Right"><Button Content="Add" Command="{Binding AddSubscriptionCommand}" Style="{StaticResource GlassButton.Cyan}" Height="28" Width="70" Margin="4,0"/><Button Content="Remove" Command="{Binding RemoveSubscriptionCommand}" Style="{StaticResource GlassButton.Red}" Height="28" Width="80"/></StackPanel></DockPanel><DataGrid Grid.Row="1" ItemsSource="{Binding Subscriptions}" SelectedItem="{Binding SelectedSubscription}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" Foreground="White"><DataGrid.Columns><DataGridTextColumn Header="Topic" Binding="{Binding Topic, UpdateSourceTrigger=PropertyChanged}" Width="*"/><DataGridTemplateColumn Header="QoS" Width="80"><DataGridTemplateColumn.CellTemplate><DataTemplate><ComboBox ItemsSource="{Binding DataContext.QualityOfServiceLevels, RelativeSource={RelativeSource AncestorType=UserControl}}" SelectedItem="{Binding QualityOfService}" Style="{StaticResource GlassComboBox}"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn><DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsEnabled}" Width="70"/></DataGrid.Columns></DataGrid><TextBlock Grid.Row="2" Text="Subscribe to one or more MQTT topic filters (use + and # wildcards)." Foreground="#8CCEDF" FontSize="12" VerticalAlignment="Center"/></Grid></Border>
                            <Border Background="#6614263A" BorderBrush="#667ECBE5" BorderThickness="1" CornerRadius="6" Padding="14"><Grid><Grid.RowDefinitions><RowDefinition Height="34"/><RowDefinition Height="260"/><RowDefinition Height="38"/></Grid.RowDefinitions><DockPanel><TextBlock Text="JSON FIELD MAPPINGS" Foreground="White" FontSize="17" FontWeight="Bold"/><StackPanel Orientation="Horizontal" DockPanel.Dock="Right"><Button Content="Add" Command="{Binding AddFieldMappingCommand}" Style="{StaticResource GlassButton.Cyan}" Height="28" Width="70" Margin="4,0"/><Button Content="Remove" Command="{Binding RemoveFieldMappingCommand}" Style="{StaticResource GlassButton.Red}" Height="28" Width="80"/></StackPanel></DockPanel><DataGrid Grid.Row="1" ItemsSource="{Binding FieldMappings}" SelectedItem="{Binding SelectedFieldMapping}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" Foreground="White"><DataGrid.Columns><DataGridTextColumn Header="Display name" Binding="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Width="*"/><DataGridTextColumn Header="JSON path" Binding="{Binding JsonPath, UpdateSourceTrigger=PropertyChanged}" Width="*"/><DataGridTextColumn Header="Unit" Binding="{Binding Unit, UpdateSourceTrigger=PropertyChanged}" Width="70"/><DataGridTextColumn Header="Topic filter" Binding="{Binding TopicFilter, UpdateSourceTrigger=PropertyChanged}" Width="*"/><DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsEnabled}" Width="70"/></DataGrid.Columns></DataGrid><TextBlock Grid.Row="2" Text="Example: display name = Voltage A, JSON path = meter.voltageA" Foreground="#8CCEDF" FontSize="12" VerticalAlignment="Center"/></Grid></Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
