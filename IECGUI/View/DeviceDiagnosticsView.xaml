<UserControl x:Class="IECGUI.View.DeviceDiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="1080" d:DesignWidth="1920">
    <UserControl.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="#B20A1A28"/>
            <Setter Property="BorderBrush" Value="#667ECBE5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="18"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F4FAFF"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#24506A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="#E6F8FF"/>
            <Setter Property="Background" Value="#102639"/>
            <Setter Property="BorderBrush" Value="#263F55"/>
            <Setter Property="Padding" Value="8,5"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#102639"/>
            <Setter Property="Foreground" Value="#E6F8FF"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#06111C" ClipToBounds="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="78"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="54"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#B214263A" BorderBrush="#667ECBE5" BorderThickness="0,0,0,1" Padding="18,0,190,0">
            <DockPanel>
                <Button DockPanel.Dock="Left" Content="BACK" Command="{Binding BackCommand}" Style="{StaticResource GlassButton.Cyan}" Width="110" Height="42" FontSize="17"/>
                <StackPanel Margin="24,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="DEVICE DIAGNOSTICS / CONTROL" FontSize="26" FontWeight="Bold"/>
                    <TextBlock Text="Read configured tags and perform guarded commissioning writes" Foreground="#9EDFF2" FontSize="14" Margin="0,4,0,0"/>
                </StackPanel>
                <TextBlock DockPanel.Dock="Right" Text="ADMIN / COMMISSIONING" Foreground="#FFD27A" FontWeight="Bold" FontSize="15" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource PanelStyle}" Margin="18,16,18,10">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Device" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}" DisplayMemberPath="MeterName" Width="310" Height="38" FontSize="16"/>
                    <Button Content="READ SELECTED" Command="{Binding ReadCommand}" Style="{StaticResource GlassButton.Cyan}" Width="160" Height="38" Margin="14,0,0,0"/>
                </StackPanel>
                <TextBlock DockPanel.Dock="Right" Text="{Binding Status}" Foreground="#BCEFFF" FontSize="15" TextWrapping="Wrap" VerticalAlignment="Center" MaxWidth="700"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource PanelStyle}" Margin="18,0,18,12" Padding="8">
            <DataGrid ItemsSource="{Binding Tags}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeight="46" Background="#102639" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Parameter" Binding="{Binding ParameterName}" Width="2*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="1.2*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Address / NodeId" Binding="{Binding Address}" Width="2*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Data Type" Binding="{Binding DataType}" Width="1.1*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="0.8*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Current Value" Binding="{Binding CurrentValue}" Width="1.5*" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Value to Write" Width="1.7*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding ValueToWrite, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="7,3" FontSize="15" Background="#0B1B2A" Foreground="White" BorderBrush="#4B86A3"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Action" Width="1.1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="WRITE" Command="{Binding DataContext.WriteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" IsEnabled="{Binding CanWrite}" Style="{StaticResource GlassButton.Cyan}" Height="31" MinWidth="92"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" Background="#122A3D" BorderBrush="#335E78" BorderThickness="1,1,1,0" Padding="18,0">
            <TextBlock Text="Writes are enabled only for configured Modbus coils/holding registers and MC/SLMP Write or ReadWrite mappings. OPC UA/DA are read-only in this first commissioning phase." Foreground="#BCEFFF" VerticalAlignment="Center" FontSize="14"/>
        </Border>
    </Grid>
</UserControl>